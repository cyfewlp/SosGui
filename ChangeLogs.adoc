= ChangeLogs
:toc: left

== 0.1.0

* [x] switch Add armor policy state not sync
* [ ] [line-through]#Set Page table for armor candidates#
+
* already replaced by `ImGuiListClipper` to scroll armor list
* [x] Sort armors
* [x] Notify Sos refresh character outfit
* [x] solve outfit solt conflicts
* [x] armor candidates list more column
* [ ] [line-through]#register new object type?#
* [x] nameless armor
+
* mod name. form id fullname?
* [x] Location auto-switch panel
* [ ] Success message
* [ ] Delete SosGui/SosUiData when menu hide?
+
* clear all data when close menu
* [x] Sort outfit list
* [x] need call Sos remove current slot armor
* [ ] Drag a child window as the main window;
* [x] Refresh read active outfit
* [x] Highlight Current actor active outfit;
* [ ] [line-through]#refactor ui translate key: by use context string to avoid specify full ket;#
* [x] fix: selected armor inconsistency with UI display;
* [ ] Enable free camera when menu open;
* [x] Support filter armor by mod name;
* [x] `DockSpace` support
* [x] try remove `GuiContext`
+
* not store `editingActor`: only passed by parameter;
* [x] refactor outfit `rename` feature:
+
* Support double click outfit name to rename?
* Support right click(context menu) to open rename input?
* [x] Bug fix: exception when refresh outfit
+
* clear `OutfitList`
* [x] Bug fix: refresh UI key not translated;
+
* CLion replaced tab to space.
* [x] Bug fix: highlight selected conflict;
* [ ] auto refresh armor
+
* when select actor
* when active/inactive outfit
* manual refresh armor
* [x] Bug fix: slot filter combo UI label untranslated;
* [x] Bug fix: slot filter combo select slot, armors view is no refresh;

=== ArmorView

* [x] support adds armor from any actor inventory and worn items
* [ ] [line-through]#support group armor by mod name#
+
* can't support multi-selection, try add combo to filter mod?
* [x] [line-through]#Add a combo to filter mod#
** `ArmorContainer`: add a new index to index armors by mod name?
* [x] ArmorView `debounce input` no longer filter armor by mod name;
* [x] ModName filter click cancel: not refresh view;
* [x] `SlotCounter` incorrect;
* [x] `add_armors_has_slot` delted:
** May add duplicated armors: armor has mutiple body slot;
** Need meet mutiple filterer;
** old code also loop all armors by `generator`, so, `reset_view` is the best way;
* [x] OutfitList as a separate window;
* [x] Bug fix: when click `Import Setting`, may have reference released outfit;
* [x] Bug fix: click mod name filter: throw exception: `invalid vector subscript`

== Features

Rename outfit::

* Double click outfit name in outfit list view
* Right click open context menu in outfit list view

Multiselect/Batch operation::

* batch mark/unmark favorites
* batch delete outfits

Outfit Armor list::

* highlight conflict slot(`BIPED_OBJECT`) with selected armors in <<armorView>>
* delete armor from outfit;
* adjust slot policy by a `combo`;

[id="armorView"]
ArmorView::

* Filter by `Slot`;
* Filter by armor name, mod name;
* batch adds all selected armors to outfit
* Support get armors from
** actor inventory;
** actor worn items;
** all registered armor in game

== Testing

* rename
* actor, outfit list and armor view context menu
* batch operator for outfit.